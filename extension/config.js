// Set this to your deployed gateway before publishing to Chrome Web Store.
export const GATEWAY_URL = "wss://YOUR_GATEWAY_DOMAIN/ws";

// Optional: if gateway enforces API key validation.
export const GATEWAY_API_KEY = "";
